---
description: Testing standards for reNgine-ng, including structure, BaseTestCase usage, TestDataGenerator, positive/negative and security-related tests, centralised invalid data, and deterministic behaviour.
alwaysApply: true
---

# reNgine-ng testing conventions

## Scope

Use this rule when writing or updating tests in `web/*/tests/` (unit, integration, functional tests).

## Test location and structure

- All tests must live under `web/<app_name>/tests/`.
- Mirror the code structure:
  - If code is in `web/<app_name>/lib_dir/`, tests must be in `web/<app_name>/tests/lib_dir/`.

## Test base classes and data

- All unit tests must use `BaseTestCase` from `utils/test_base.py` at the project root.
- Use `TestDataGenerator` to create test data; do not hand-roll ad-hoc factories everywhere.
- Anonymise all test data (IP, hostnames, DNS, emails, etc.) so no real data appears in tests.

### BaseTestCase behaviour

- `BaseTestCase` sets up a minimal authenticated user and project environment using `TestDataGenerator`.
- It also:
  - Temporarily replaces `on_user_logged_in` with a no-op to avoid side effects during tests.
  - Disables logging with `logging.disable(logging.CRITICAL)` so test output stays clean.
- Tests that need different login behaviour or visible logs must do so carefully (for example with a dedicated base class or explicit re-enabling) without breaking the default contract of `BaseTestCase`.

### Example – using BaseTestCase and TestDataGenerator

```python
from utils.test_base import BaseTestCase, TestDataGenerator


class TargetServiceTestCase(BaseTestCase):
    def setUp(self) -> None:
        super().setUp()
        self.data_gen = TestDataGenerator()
        self.target = self.data_gen.create_target()

    def test_summary_is_generated(self) -> None:
        summary = self.target.get_summary()
        self.assertIsNotNone(summary)
```

## Positive and negative test data

- For each behaviour under test, include both **valid** (happy path) and **invalid or malicious** cases. Assert the expected outcome: success for valid inputs, rejection or validation error for invalid ones.
- **Valid data**: use only `TestDataGenerator` from `utils.test_utils`; do not hand-roll fixtures.
- **Invalid or malicious data**: use the centralised constants from `utils.test_utils` — `BadPathSamples` and `BadUrlSamples` — and do not duplicate magic strings across test files. If you need additional bad-input constants, add them there and reuse.

## Security-related tests

- For code that handles paths, URLs, user input, API responses, or displayed content, add tests that verify **rejection** of invalid or malicious inputs (e.g. path traversal, dangerous URL schemes).
- Refer to the rule **rengine-ng-security.mdc** for the security rules that must be enforced; tests should assert that those rules are upheld. Do not duplicate the rule text; only reference it and add tests that validate the behaviour.

## Determinism and isolation

- Tests must be deterministic and independent of timing.
- Avoid relying on `sleep`, real network calls, or external services.
- Cover edge cases, especially around error handling.
- Do not duplicate test data: valid data comes from `TestDataGenerator`, invalid/malicious data from `BadPathSamples` / `BadUrlSamples` in `utils.test_utils`.

## Running tests

- When models have changed, run the full test suite without `KEEPDB`:

```bash
make test
```

- When models have not changed, you can keep the DB to speed up runs:

```bash
make test KEEPDB=1
```

- To run only specific test(s), use `test-only` with the full dotted path(s):

```bash
make test-only TESTS="reNgine.tests.services.repositories.test_domain_repository.DomainRepositoryFindingScopeFilterTest.test_save_raw_whois_ip_as_domain_returns_none" KEEPDB=1 VERBOSITY=2
```

Examples for recently added areas:

```bash
make test-only TESTS="api.tests.test_llm_attack_surface api.tests.test_subdomain_ip_xor recon_note.tests.test_recon_note reNgine.tests.llm.test_attack_surface_context" KEEPDB=1 VERBOSITY=2
```

  Multiple tests: pass a space-separated list of dotted paths as `TESTS="path1 path2 path3"`. Same options as `make test` apply (`KEEPDB=1`, `VERBOSITY=1|2|3`).

- Other useful commands:

```bash
make test VERBOSITY=2
make test-app APPS=api VERBOSITY=3 KEEPDB=1
make test-verbose  # equivalent to make test VERBOSITY=2
```

## Temporary test files

- If you generate temporary Python test files or validation scripts (outside of unit/integration/functional tests), delete them once they are no longer needed.
